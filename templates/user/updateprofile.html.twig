{% extends 'base.html.twig' %}
{% form_theme userUpdateForm 'bootstrap_4_horizontal_layout.html.twig' %}


{% block title %}Modifier profil{% endblock %}

{% block body %}
    <br>
    <h3 id="textcenter">Modifier mon profil</h3>
    <br>
<div class="row">

    <div class="col-8 offset-2">
    {{ form_start(userUpdateForm) }}
    {{ form_widget(userUpdateForm) }}
        <div class="row col-sm-12">
            <div class="row col-sm-6">
            <button id="btn-mod-profil" class="btn  btn-outline-dark" type="submit">Enregistrer</button>
            </div>
            <div class="row col-sm-6">
                <a class="btn  btn-outline-dark" href="{{ path('sortie_list') }}">Annuler</a>
            </div>

        </div>
    {{ form_end(userUpdateForm) }}

    </div>

</div>

{% endblock %}
